{
  "name": "workspace",
  "version": "1.0.0",
  "description": "Turns a public GitHub repository into an explorable map of its architecture: files, dependencies, semantic layers, and the reasoning behind changes, drawn from its pull requests.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "repo-explorer-frontend": "file:frontend"
  }
}
